#include<stdio.h>
#include<conio.h>
int count(char arr[])
{
    int c=0,i;
    for( i=0;arr[i]!='\0';i++)
    {
    
    }
    return i;
}
int main()
{ char name[100];
 printf("enter the string");
 scanf("%s",name);
printf("length is %d",count(name));
return 0;
}